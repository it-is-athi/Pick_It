# Pick_It

# BACKEND
Run: 
1. npm init - to set up the node package manager.
2. npm install express
3. npm install express-session
4. npm install dotenv
5. npm install cors
6. npm install node-cron

# FRONTEND
1. npm install
2. npm run dev

athi-mutta
muttamoonju-athi